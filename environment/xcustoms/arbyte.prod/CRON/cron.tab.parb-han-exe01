# -*- mode: sh -*- #

# All times UTC (In Sommer time: CET - 2 hours)

MAILTO=pirnog@gmail.com

# Set this variable for running X-based applications
#DISPLAY=:0

# Each weekday, start the binaries
30 05 * * Mon-Fri bash --login -c ". ~/.bash_profile; cd ~/live/bin && ~/CRON/runVPStratLauncher.sh" > /dev/null 2>&1

# Each day (including week-end) update configs for the next day
30 23 * * Sun-Sat bash --login -c '. ~/.bash_profile; ~/CRON/updateConfigsForTomorrow.sh' > /dev/null 2>&1
