# -*- mode: sh -*- #

# All times UTC (In Sommer time: CET - 2 hours)

MAILTO=pirnog@gmail.com

# Set this variable for running X-based applications
#DISPLAY=:0

# Each weekday, start the binary at 00:15 UTC
# * * * * Mon-Fri bash --login -c ". ~/.bash_profile; cd ~/live/bin && ~/bin/restartYang.sh" > yang_cron_test.log 2>&1
