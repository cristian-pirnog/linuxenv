# -*- mode: sh -*- #

section: screens
    baar:
    crpi-notebook:
end

section: aliases
    crpi-notebook:
         10.199.10.90 
    baar:
         10.199.10.10
end

section: links
    baar:
        right = crpi-notebook
        left  = crpi-notebook
    crpi-notebook:
         left  = baar
         right = baar
end


section: options
    heartbeat = 1800
    keystroke(Alt+Left) = switchInDirection(left)
    keystroke(Alt+Right) = switchInDirection(right)
    switchCorners = all
    switchCornerSize = 50
end
